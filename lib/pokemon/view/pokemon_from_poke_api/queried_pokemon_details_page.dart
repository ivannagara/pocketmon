import 'package:flutter/material.dart';

class QueriedPokemonDetailsPage extends StatelessWidget {
  const QueriedPokemonDetailsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Search Results'),
        centerTitle: true,
      ),
      body: Container(),
    );
  }
}